# NutriMind AI — runtime dependencies, pinned.
#
# Exact versions, because "it worked yesterday" has to stay reproducible: an
# upstream release must never be able to break CI and production on the same day
# with no change on our side. Upgrades happen deliberately, via Dependabot PRs.
#
# Edit requirements.in, not this file, then regenerate (see that file's header).
# Test and lint tooling lives in requirements-dev.txt so production images stay
# lean and never ship a test runner.
#
# Verified together: the full suite passes on this exact set.
#
# Scope note: these are the DIRECT dependencies. Transitive packages are still
# resolved at install time. Full transitive + hash locking is worth adding, but
# it has to be generated on the deployment Python (3.11) to be correct, so it is
# deferred to the deployment milestone rather than guessed at here.

# Web framework
flask==3.1.3
flask-sqlalchemy==3.1.1
flask-wtf==1.3.0
flask-login==0.6.3
python-dotenv==1.2.2

# Database schema migrations. alembic arrives as a transitive dependency and is
# deliberately left unpinned, matching the convention above: direct dependencies
# are pinned, transitive ones are resolved.
flask-migrate==4.1.0

# PostgreSQL driver (see requirements.in for why it is not an optional extra).
psycopg[binary]==3.3.4

# IBM watsonx.ai
# This package sets the project's Python floor and ceiling: it declares
# Requires-Python >=3.11,<3.15, and every release caps the upper bound. Older
# releases cap it lower (1.3.x is <3.14, 1.2.x is <3.13), so the SDK version and
# the interpreter version have to be chosen together — see requirements.in.
ibm-watsonx-ai==1.6.0

# RAG
chromadb==1.5.9
pypdf==6.13.1

# PDF export
reportlab==4.5.1

# Optional local embedding fallback (heavy: pulls PyTorch). Install only if you
# want offline embeddings: pip install sentence-transformers
# sentence-transformers==3.0.1
