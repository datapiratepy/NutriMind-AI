# NutriMind AI — dependencies (exact versions pinned during Phase 4 install)

# Web framework
flask>=3.0
flask-sqlalchemy>=3.1
flask-wtf>=1.2
python-dotenv>=1.0

# IBM watsonx.ai
ibm-watsonx-ai>=1.1

# RAG
chromadb>=0.5
pypdf>=4.0

# Optional local embedding fallback (heavy: pulls PyTorch).
# Install only if you want offline embeddings: pip install sentence-transformers
# sentence-transformers>=3.0

# PDF export (Phase 8)
reportlab>=4.0

# Testing
pytest>=8.0
pytest-cov>=5.0
